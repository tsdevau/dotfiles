# style name or JSON path (default "auto")
style: /Users/tpstech/.local/share/chezmoi/private_dot_config/private_glow/catppuccin-mocha.json
# mouse support (TUI-mode only)
mouse: true
# use pager to display markdown
pager: false
# word-wrap at width
width: 120
# show all files, including hidden and ignored.
all: false
